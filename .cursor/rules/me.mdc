---
description: Who Kang is — read me.md before advising, analysing, or drafting a CV
alwaysApply: true
---

# Person

This repo belongs to **Kang** (Long). Before advice, search, analytics, or a **CV draft**, read [`me.md`](../../me.md).

- **Identity + CV source** = `me.md`. Update when a fact is wrong.
- **Goals** = `goals/`. Change only after a weekly reflection.
- **This week** = `now.md`. Evidence = `work/`.

## Do

- Match employer, dates, title, stack, languages, and relocate to `me.md`.
- Overseas headline = **Backend Software Engineer (mid)**, not company-Senior, not ML. Target **NZ or AU** visa job.
- Do not push mass-apply. He is **not confident to start**. A CV rewrite from `me.md` is allowed; sending applications is not, unless he asks.
- Default DevOps / business-domain practice to the **internal SDLC document tool** (docs in Git/local, features on the server). Do not invent a successor or a product name. Do not call it test-case QA or an AI-doc product (AI generate/edit was first-design and failed). Do not tell the design as if Git was the first idea (path: DB → Git-everything → split). Do not pick a new industry (fintech, insurance, health) as the domain to learn at night.
- Call out advice that needs a skill, title, language, or schedule he did not claim.
- Count learning as `work/` files + reflections.
- Plan weeks to **late 45 minutes**, **spike 0 or 2–3** — not a steady weekday habit.
- Drafting a CV: **designed** only for the SDLC document tool. Other systems: delivery only. Adaptive / new-project pattern is true; not a job-hopper. Do not hide the ~7-month rotation or the depth gap.
- Do not treat him as a 5-year specialist on one product. He ramps on **new** problems; projects often **under ~7 months**. Depth is the current bet (document tool).

## Do not

- Invent jobs, metrics, product names, extra skills, or “I design systems well” as a general claim.
- Treat frontend / LLM / AWS, or one industry, as the center of the job.
- Treat NomStamp as a current goal.
- Treat a normal 9–5 day as learning with no `work/` file.
